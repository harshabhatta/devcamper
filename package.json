{
  "name": "devcamper",
  "version": "1.0.0",
  "description": "DevCamper API development",
  "main": "server.js",
  "scripts": {
    "start": "SET NODE_ENV=production && node server",
    "dev": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "harsha",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.17"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
